{
  "type": "module",
  "private": true,
  "workspaces": [
    "packages/*",
    "packages/@*/*",
    "examples"
  ],
  "scripts": {
    "test": "vitest",
    "clean": "npm exec -ws -- rimraf ./lib",
    "build": "tsc -b tsconfig.build.json",
    "build:watch": "tsc -b tsconfig.build.json --watch",
    "format": "prettier --write '**/**/*.{ts,json}'",
    "lint": "prettier --check '**/**/*.{ts,json}'"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.3.1",
    "prettier": "2.8.8",
    "rimraf": "^5.0.1",
    "vitest": "^0.32.2"
  }
}
